@model bool
@{
    var PropertyName = ViewData.ModelExplorer.Metadata.PropertyName;
    var v = Model.ToString().ToLower();
    string c = Model ? "checked" : "";
}


<input type="checkbox" @c value="true" data-toggle="toggle" data-onstyle="success" data-offstyle="danger" data-on="Leaning" data-off="Suspended"
       id="@PropertyName" name="@PropertyName">

